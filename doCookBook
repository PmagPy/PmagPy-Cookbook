#git checkout gh-pages     # if you’re not already on that branch
git pull # to make sure your were on the current version
htlatex PmagPy "xhtml,1,sections+,charset=utf-8" -cmozhtf 
# do htlatex on any other .tex files that were modified,  for example:
# htlatex osx_developer.tex
git commit -a -m “made some changes”
git push
