# uncomment the customlinks import and all the \customlink lines
# then run: 
htlatex PmagPy "html,1,sections+"
#The first time do this:
#
#git checkout gh-pages
#git merge master
git push origin gh-pages
#git checkout master
#
#Afterwards:   
#git commit -a
# git push
